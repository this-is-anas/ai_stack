import 'package:flutter/material.dart';
class PromptLibrary extends StatelessWidget {
  const PromptLibrary({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Prompt Library Page"),
      ),
    );
  }
}
